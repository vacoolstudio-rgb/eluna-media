/// Kept in lockstep with pubspec.yaml's `version:` by hand — the what's-new
/// dialog keys off it, and reading the pubspec at runtime would drag in a
/// package for one string.
const String kAppVersion = '0.2.0';
