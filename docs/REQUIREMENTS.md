# Eluna Media — Требования (v1.0)

Дата: 2026-07-12. Основано на анализе ~12 конкурентов (Google Play / App Store),
тем жалоб из отзывов, и паттернов приложений семейства Eluna (Reader, Cycle).

## 1. Позиционирование

Полностью приватный офлайн-конвертер фото/видео/аудио для Android и iOS.
Один экран — результат («сжать», «уложиться в размер», «в MP4», «вытащить звук»),
вся мощность FFmpeg — на один тап глубже. Честная монетизация.

**Формула из исследования:** UX уровня Panda (4.67★) + мощность уровня
Media Converter (3.4★ из-за UX) + приватность уровня Kompresso + честность YouCut.

## 2. Непреложные принципы (нарушать нельзя)

| # | Принцип |
|---|---------|
| P1 | Без подписок, без пейволов на функции, без вотермарок. Батч бесплатен навсегда. |
| P2 | Ровно один anchored-adaptive баннер AdMob, только неперсонализированный (`npa`), тестовые ID до релиза. |
| P3 | Баннер никогда не показывается, пока идёт конверсия, и первые 14 дней после установки. |
| P4 | Единственная покупка — разовое «убрать рекламу» (пока тестовый стаб, без платных SDK). |
| P5 | Без бэкенда, регистрации, аналитики, crash-SDK. Сеть — только для баннера, и это честно объяснено в приложении. |
| P6 | Только бесплатные open-source библиотеки. |
| P7 | Реклама/оплата — за абстракциями, боевые ID подключаются одной правкой конфига. |

## 3. Что показало исследование отзывов (→ требование)

| Жалоба/желание пользователей (частота × тяжесть) | Требование |
|---|---|
| Реклама прерывает конверсию (жалоба №1 в категории) | P3 + баннер схлопывается при ошибке загрузки |
| Подписочные ловушки, «$9.99/неделя» | P1, P4 |
| «Сжать под Discord/почту» — никто не умеет | **Fit-to-size: целевой размер в МБ** (пресеты 10/25/50 МБ + свой), расчёт битрейта из длительности |
| Жаргон кодеков без подсказок (CRF? bitrate?) | Simple-режим с пресетами-результатами; Advanced — на один тап глубже |
| Батч за пейволом у всех | Батч бесплатно (уже есть) — вынести как обещание в UI |
| Жор батареи / перегрев | Режим энергопотребления (Эконом/Баланс/Качество), предупреждение при старте на низком заряде, последовательная очередь (уже есть) |
| Краши на больших файлах, батч падает целиком | Изоляция ошибок по файлу (есть), кнопка Retry для упавших, понятные тексты ошибок |
| «Не могу найти файл после конверсии» | Кнопка «Сохранить в галерею/загрузки» + Share; итоговый размер до/после (есть) |
| HEVC не играет на старом устройстве | Пресет «Совместимость (MP4 + H.264)» |
| Субтитры теряются при MKV→MP4 | Тумблер «Сохранить субтитры» (mov_text для MP4, copy для MKV) |
| Метаданные: хочу выбирать | Тумблер «удалить метаданные» остаётся, по умолчанию ВКЛ (приватность) |
| Повернуть/отразить видео | Трансформации: поворот 90/180/270, зеркало |
| Скорость воспроизведения | Изменение скорости 0.5×–2× (видео+аудио) |
| Обрезка по времени | Trim-UI (домен уже поддерживает) |
| Видео → GIF | Уже есть — вынести в пресеты |
| «Открыть с помощью» из галереи/файлmenedžера | Приём файлов через share-intent / Open with |
| Тёмная тема, OLED | Темы: системная/светлая/тёмная + чистый чёрный OLED |

## 4. Функциональные требования

### 4.1 Конвертация (ядро — есть, расширить)
- FR-1. Видео: MP4/MKV/MOV/WebM/AVI; H.264/H.265/VP9/MPEG-4/remux. (есть)
- FR-2. Аудио: MP3/M4A/WAV/FLAC/OGG/Opus, извлечение из видео. (есть)
- FR-3. Изображения: JPEG/PNG/WebP/BMP/TIFF/GIF. (есть)
- FR-4. **Целевой размер файла**: пользователь задаёт МБ; битрейт = размер×8×0.93/длительность − аудио-битрейт, с maxrate/bufsize; предупреждение при недостижимом target.
- FR-5. **Оценка размера результата** до старта (для битрейт/target-режимов — точная, для CRF — не показывается).
- FR-6. Трансформации: поворот, зеркало, скорость 0.5–2×.
- FR-7. Субтитры: сохранять при конверсии в MP4/MKV (опция).
- FR-8. Trim: начало/конец для одиночного файла.
- FR-9. Retry упавшего задания в один тап.
- FR-10. Сохранение результата в галерею (фото/видео) или системную папку; Share. Аудио — Share/копия в папку загрузок.

### 4.2 UX-режимы
- FR-11. **Simple (по умолчанию)**: карточки-пресеты: «Сжать видео», «Уложиться в размер», «Совместимый MP4», «Извлечь звук (MP3)», «GIF из видео», «Сжать фото», «WebP из фото». Выбор файлов → пресет → Convert.
- FR-12. **Advanced**: все текущие контролы + новые (FR-4..FR-8). Переключатель Simple/Advanced запоминается.
- FR-13. Файлы, добавленные в любом режиме, показываются списком с именем, размером, длительностью.

### 4.3 Монетизация (тестовая)
- FR-14. Баннер AdMob: тестовые unit ID, `nonPersonalizedAdsOnly`, grace 14 дней, скрыт при активной очереди и для Pro, collapse при ошибке, refresh не чаще 2 мин и только в форграунде.
- FR-15. Экран «Убрать рекламу»: разовая цена, кнопки «Купить» и «Восстановить». Пока стаб (успех в debug), интерфейс под `in_app_purchase`.

### 4.4 Доверие и polish
- FR-16. Privacy-intro при первом запуске (3 обещания: офлайн-конверсия, ноль телеметрии, метаданные под контролем) — один экран, кнопка «Понятно».
- FR-17. Экран «Сеть и приватность» в настройках: полный аудит (баннер — единственный сетевой канал; конверсия работает и в авиарежиме).
- FR-18. Rate-us: день 7 и 14 от первого запуска, при ≥2 успешных конверсиях, не чаще 1/день, iOS — всегда нативный промпт, Android — 5★ → Play, <5★ → email.
- FR-19. Хаптика на старт/успех/ошибку конверсии (отключаемая).
- FR-20. Глобальный обработчик ошибок: дружелюбный экран вместо краша, без отправки куда-либо.
- FR-21. Undo-снекбары для удаления заданий/очистки очереди.
- FR-22. Настройки: Внешний вид (тема/OLED/язык) · Конвертация (режим, энергорежим) · Приватность (метаданные, аудит сети) · Поддержка (оценить, поделиться, убрать рекламу, лицензии, версия).
- FR-23. Локализация: EN + RU полностью; каркас готов к расширению (ARB).

### 4.5 Платформы
- FR-24. Android 7+ (minSdk 24), iOS 14+. Телефоны и планшеты (NavigationRail ≥720dp — есть), проверка на 320dp и 1.5× шрифте (есть в тестах — сохранить).
- FR-25. Приём файлов из других приложений (Share/Open with) — Android intent-filters + iOS.

## 5. Нефункциональные требования

- NFR-1. **Энергия**: очередь строго последовательная; энергорежим меняет пресет x264/x265 (Эконом=ultrafast, Баланс=veryfast, Качество=medium); при <20% заряда без зарядки — предупреждающий диалог перед стартом; баннер не грузится во время конверсии.
- NFR-2. **Производительность UI**: без jank — редкие ребилды (Riverpod select уже используется), троттлинг прогресса (есть), const-виджеты.
- NFR-3. **Надёжность**: очередь переживает смерть процесса (есть), частичные файлы удаляются (есть), один упавший файл не роняет батч (есть).
- NFR-4. **Приватность**: единственное сетевое использование — SDK баннера; конверсия не требует сети; никакие файлы/метрики не покидают устройство.
- NFR-5. Все host-тесты проходят; новые модули (аргументы FFmpeg, расчёт target-битрейта, гейтинг рекламы, майлстоуны rate-us) покрыты unit-тестами.
- NFR-6. Лицензия приложения остаётся GPL v3 (full-gpl FFmpeg) — все новые зависимости совместимы и бесплатны.

## 6. Вне скоупа v1.0 (задокументировать)
- AV1-энкодер и HEIC (нет в бандле FFmpeg — требует пересборки).
- Фоновая конверсия на iOS (ограничение ОС — как у всех).
- Мерж видео, наложение аудио, встроенный плеер-превью.
- Массовые переводы (>EN/RU) — фаза 2.
- Боевые ID AdMob и реальный биллинг — подключает владелец.
